procedure main
   ? "esta string vai ser escrita " + ;
      "em tres linhas " + ;
      "mas aparece em apenas uma linhas."

   ? "ola pessoal!"; ? "Como vocês estao?"; ? "Espero que bem."

   ? "25" + "25"
   ? 25 + 25
return


// não precisa usar ; no final das frases
// O ; é usados para quebrar linhas
