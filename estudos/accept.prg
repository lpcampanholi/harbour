procedure main
   
   local cNome
   local nIdade

   ? "Digite seu nome"
   cNome := input()

   ? "Digite sua idade"
   nIdade := input()

   ? "Seu nome e:", cNome
   ? "Sua idade e:", nIdade

return
